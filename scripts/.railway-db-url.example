# Put your Railway Postgres URL on the next line (public host from Dashboard →
# Postgres → Connect). No quotes. Lines starting with # are ignored.
#
# Do not use postgres.railway.internal from your Mac unless you use a tunnel.

postgresql://USER:PASSWORD@PUBLIC_HOST:5432/railway
